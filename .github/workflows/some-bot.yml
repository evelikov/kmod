name: magic

on:
  issue_comment:
    types: [created]

# Need write perm, in order to push the ammended commits
permissions:
  content: write-all

jobs:
  something:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/github-script@v7
        if: github.event.action != 'closed'
        with:
          script:
            console.log(context)



